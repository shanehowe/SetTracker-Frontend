import React from 'react';
import { ScreenProps } from '../interfaces';
import { Text } from 'react-native-paper';

export const WorkoutFoldersScreen = ({ navigation }: ScreenProps) => {
    return (
        <Text>Workout Folders</Text>
    );
};